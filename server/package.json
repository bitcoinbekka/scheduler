{
  "name": "plebeian-scheduler-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hostable backend for Plebeian Scheduler — stores pre-signed Nostr events and publishes them at the scheduled time.",
  "license": "GPL-3.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node scheduler-server.mjs"
  }
}
